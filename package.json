{
    "name": "kids-remote-root",
    "version": "1.0.0",
    "description": "Root package for Kids Remote project with Backend and Frontend",
    "scripts": {
        "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "build": "npm run build --prefix frontend && npm run build --prefix backend",
        "start": "cd backend && node dist/index.js",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    },
    "author": "",
    "license": "ISC"
}